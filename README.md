It's test task.

## Running

1. You need to open this project in terminal.

2. Install node modules:

with Yarn:

`yarn`

with NPM:

`npm install`

3. Build

with Yarn:

`yarn build`

with NPM:

`npm run build`
